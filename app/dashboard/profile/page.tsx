import { ProfileView } from "@/components/profile/ProfileView";

export const metadata = {
  title: "Profile — Sawy Academy",
};

export default function DashboardProfilePage() {
  return <ProfileView />;
}
